# ============================================================
#  .env.example — copia este archivo como .env y llena los datos
#  ¡NUNCA subas el archivo .env a Git!
# ============================================================

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=tu_password_aqui
DB_NAME=ferreteria

# Puerto del servidor (Vercel lo ignora, usa el suyo)
PORT=3000
